function sum(a, b) {
  // чтобы возвращала результат выполнения надо написать return
  return a + b;
  // после return тело функции перестает выполняться
}
// console.log - что-то выводит в консоль
const result = sum(100, 200);
console.log("30 - ", result);

function numbers(x, y) {
  result = x + y;
  console.log(result);
}
